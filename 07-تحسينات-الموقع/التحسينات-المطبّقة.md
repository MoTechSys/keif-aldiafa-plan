# 🔧 تحسينات موقع كيف الضيافة (نسخة معزولة — لم تُمس الأصلية)

> ⚠️ **مهم:** هذا مجلد عمل منفصل تماماً. المستودع الأصلي `keif-v2` (المربوط بالدومين) **لم يُعدّل إطلاقاً**.
> التاريخ: 2026-07-21 | البناء: ✅ نجح (next build EXIT 0)

## 🔍 تصحيح مهم لتقرير التحليل
تقرير الـ5 أدوار **بالغ في بعض المشاكل** — بفحص الكود الفعلي تبيّن أنها غير موجودة:
- ❌ «النموذج مخفي بالـJS» → **غير صحيح**: النموذج `motion.form` بـ `animate` (يظهر دائماً، مجرد fade-in).
- ❌ «لا توجد `<label>`» → **غير صحيح**: كل حقل له `<label htmlFor>` صحيح.
- ❌ «زر الهيدر بلا رسالة مُعبّأة» → **غير صحيح**: `useWhatsAppUrl` يعطي رسالة سياقية لكل صفحة.

**الموقع أنظف وأحترف مما وصفه التقرير.** ركّزنا على التحسينات الحقيقية فقط.

## ✅ التحسينات المطبّقة فعلياً (والبناء نجح)
1. **تحقّق رقم الجوال السعودي** (`ContactClient.tsx`):
   - إضافة `isValidSaudiPhone` — يتحقق `05+8` أو `+9665` أو `9665`.
   - رسالة خطأ عربية واضحة + حد أقصى للأحرف + `aria-invalid`/`aria-describedby` (وصول).
   - يمنع إرسال أرقام خاطئة تضيّع العملاء.
2. **احترام `prefers-reduced-motion`** (`globals.css`):
   - تعطيل الأنميشن للمستخدمين الحساسين للحركة (توصية وصول من التقرير).

## 🔲 تحسينات تحتاج قرارك (لم تُطبّق — تتطلب موارد خارجية)
1. **البريد `info@keifaldiafa.com`** بدل gmail — **لم أغيّره** لأني لا أعرف إن كان الإيميل مفعّلاً فعلاً؛ تغييره قبل تفعيله = ضياع رسائل العملاء. فعّله أولاً ثم غيّره.
2. **صور جديدة بلا كمامات** — تحتاج جلسة تصوير (طلبناها من العميل).
3. **إشعار كوكيز (PDPL)** — يحتاج مكوّن consent + ربط بسكربتات التحليلات.
4. **توجيه edge لأقرب POP** — إعداد Vercel (يحتاج لوحة Vercel).

## 📦 طريقة التطبيق على الموقع الفعلي
هذه التعديلات في `src/app/contact/ContactClient.tsx` و `src/styles/globals.css` فقط. لتطبيقها:
1. راجع الفرق (diff) للملفين.
2. طبّقها على `keif-v2` عبر فرع جديد (branch) + Pull Request، لا مباشرة على master.
3. بعد المراجعة والبناء، ادمج وانشر.
