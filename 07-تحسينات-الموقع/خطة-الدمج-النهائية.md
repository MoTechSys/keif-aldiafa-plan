# 🔀 خطة دمج تحسينات الموقع — كيف الضيافة

> النسخة المعزولة: /home/work/keif-v2-improvements/ | الأصلي keif-v2 (الدومين) لم يُمس.
> كل التعديلات تحقّقت ببناء ناجح (next build EXIT 0).

## ✅ التحسينات المطبّقة (متحقّقة)

### أ) بواسطة الوكيل الرئيسي
1. **تحقّق رقم الجوال السعودي** (ContactClient.tsx) — pattern + رسالة خطأ + aria.
2. **prefers-reduced-motion** (globals.css) — وصول.
3. **مكوّن إشعار الكوكيز PDPL** (components/CookieConsent.tsx) — جاهز، يحتاج ربط بـlayout + consent gating للتحليلات.

### ب) بواسطة وكيل SEO (opus-4-8) — البناء EXIT 0
- أحياء جدة (الفيحاء/المحمدية/الزهراء) في صفحات المدن — تلتقط بحث الإعلان المهدور.
- إثراء Service schema (areaServed هرمي + قناة واتساب + باقات حقيقية، بلا أسعار مختلقة).
- تمييز أوصاف الميتا لكل مدينة (تجنّب thin content).
- ✅ رفض AggregateRating الوهمي (نزاهة — تجنّب عقوبة جوجل).
- الملفات: lib/localPages.ts, lib/schema.ts, lib/localContent.tsx, app/[serviceCity]/page.tsx

### ج) بواسطة وكيل التحويل (opus-4-8)
- (قيد الإكمال — رسائل واتساب للمدن + CTA المعرض + الزر العائم)

## 📋 خطوات التطبيق على الموقع الحي (آمنة)
1. **لا تدمج مباشرة على master.** أنشئ فرعاً: `git checkout -b improvements-2026-07`.
2. راجع diff كل ملف.
3. طبّق التعديلات، ثم `npm run build` محلياً.
4. Pull Request → مراجعة → دمج → نشر Vercel.

## 🔲 يحتاج قرار/موارد خارجية
- ربط CookieConsent + consent gating لسكربتات GA/Meta/TikTok.
- بريد info@keifaldiafa.com (فعّله قبل تغييره من gmail).
- صور جديدة بلا كمامات (تصوير).
- توجيه Vercel edge لأقرب POP للسعودية.
